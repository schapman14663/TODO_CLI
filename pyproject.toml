[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "TODO_CLI"
version = "0.0.1"
requires-python = ">= 3.11"

[project.scripts]
TODO = "TODO_CLI:main"
